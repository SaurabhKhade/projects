{
  "name": "calculator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export && cd out && touch .nojekyll && git init && git add . && git commit -m 'uploading from termux' && git remote add origin https://github.com/SaurabhKhade/math-calculator.git && git branch gh-pages && git checkout gh-pages && git push origin gh-pages --force",
    "start": "next start"
  },
  "dependencies": {
    "next": "10.1.3",
    "nextjs-progressbar": "^0.0.9",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  }
}
